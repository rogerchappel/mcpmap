{
  "mcp": {
    "servers": {
      "sqlite": {
        "command": "uvx",
        "args": ["mcp-server-sqlite", "--db-path", "./data/app.db"],
        "cwd": "workspace",
        "env": {
          "DATABASE_URL": "sqlite://data/app.db"
        },
        "tools": {
          "query": {},
          "schema": {}
        }
      }
    }
  }
}
